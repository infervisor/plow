{
  "patch": "diff --git a/crates/devgen/src/dense_cublaslt.rs b/crates/devgen/src/dense_cublaslt.rs\nindex d79afa23..87270e10 100644\n--- a/crates/devgen/src/dense_cublaslt.rs\n+++ b/crates/devgen/src/dense_cublaslt.rs\n@@ -269,6 +269,19 @@ mod tests {\n             metadata.objects[&plow_asset::segment_roles::NATIVE_DECODE_TC].sha256,\n             Some(plow_asset::decode_objects::image_sha256(&image))\n         );\n+        let mut mixed = model();\n+        mixed.progs[1].insts[1].op = DevOp::GemvGluFp8 as u16;\n+        let original: Vec<_> = mixed.progs[1].insts.iter().map(|d| d.pack()).collect();\n+        let section = apply_native(&mut mixed, &directory.join(\"model.pkt\")).unwrap();\n+        let metadata = plow_asset::segment_roles::SegmentRoles::from_bytes(&section.data).unwrap();\n+        assert_eq!(\n+            metadata.programs[0].roles,\n+            [0, plow_asset::segment_roles::NATIVE_DECODE_TC]\n+        );\n+        assert_eq!(\n+            mixed.progs[1].insts.iter().map(|d| d.pack()).collect::<Vec<_>>(),\n+            original\n+        );\n         let mut wide = model_rows(&[128, 1, 16, 32]);\n         assert!(apply_native(&mut wide, &directory.join(\"model.pkt\"))\n             .err()\ndiff --git a/crates/devgen/src/lib.rs b/crates/devgen/src/lib.rs\nindex f75295b0..2327bb98 100644\n--- a/crates/devgen/src/lib.rs\n+++ b/crates/devgen/src/lib.rs\n@@ -5222,7 +5222,7 @@ fn emit_phase(\n         let glu_fused = gemv_family\n             && gemv_fused_input_fits(amd, t, c.hidden)\n             && !emit_config::active().decode_cublaslt\n-            && !emit_config::active().decode_native_tc;\n+            && (!emit_config::active().decode_native_tc || fp8);\n         let gemm_glu = !gemv_family && glu_fusion_wins(tg, inter_l, c.hidden, n_cu);\n         // w8a8: quant the (hidden-width) pre-FF norm output feeding gate/up. Reuses xqh/ash (q/k/v\n         // already consumed them; the c_pf\u2192o_proj\u2192flash\u2192qkv chain serializes the reuse). Inert\n@@ -8531,8 +8531,9 @@ fn emit_dense_gqa(\n     let mut sections = Vec::new();\n     if ecfg.decode_cublaslt || ecfg.decode_native_tc {\n         assert!(\n-            !fp8 && !c.moe && !amd,\n-            \"Gemma cuBLASLt decode requires dense BF16 CUDA\"\n+            !c.moe && !amd && !ecfg.mxfp4\n+                && (!fp8 || (ecfg.decode_native_tc && arch == \"sm_90a\")),\n+            \"decode projection segments require dense BF16 CUDA or native SM90 BF16 heads with FP8 layers\"\n         );\n         sections.push(\n             if ecfg.decode_native_tc {\n"
}
