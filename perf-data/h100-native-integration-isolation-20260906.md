# H100 native integration isolation

No new production kernel default is promoted. E3 is the strongest current batching candidate; attention integration still requires numerical qualification.

- E1 scalarGF16 matches all30full-model cross-arm rows exactly at1K/8K/32K, with unchangedNS33, arena and staging. PV and QK-only candidates fail the fixed1% full-logit gate. This isolates the change to the tensor-core arithmetic path; grouping and equal-arena controls are exact. An independent model oracle may still be needed to assess which accumulation path is closer to the desired model output.
- E4 four real-layer5 saved-input replays pass against the captured deployed output andFA4. Direct actual candidate-role capture also passes: Q/K/V exact; attention output relativeL2 .00002512, with212/1048576BF16values differing. The first full-model gate remains failed. This single-layer result does not explain or excuse later divergence; later prefixes are next.
- E6 strict unfiltered sanitizer validation accepts only the two proven optional packet-hash symbol probes, and rejects unknown records/callsites. Both raw sanitizer exits remain99. All captured output rows match controls exactly. The16KiBprefetch window lowers persistent warm-decode medians0.78–0.96% in two opposing orders. This is a small diagnostic gain, not a serving win; no further cap sweep is prioritized overE3/E4.
- E3 fixed132compute-grid tests pass1920fullcorrectnessrows,224memcheckrows and756timingrows. Median posthoc speedups over scalar atM4/8/16 are1.462/1.963/3.502×; vs cuBLASLt they are.972/.949/.956×. Repeat0-selected splits evaluated onrepeat1 give similar results. The M16,N3840,K15360 fixture selectsS8 in both analyses:68.184us vs392.392us scalar and66.336us cuBLASLt. These intervals include zero/compute/finalize, not interpreter gates or residency. All device code is unchanged from the earlier adaptive-grid experiment.
- E3 packet prototype is being built in an isolated source copy with explicit zero, split-K and finalizer instructions. Early broad build is216registers vs200control with no spills; GPU occupancy, scratch lifecycle, whole-block numerics and serving remain required. No new model-specific runtime switch is proposed.

The actual Qwen wrapper invalidated an early RMSNorm-rounding suspicion: it suppliesFP32gamma+1, so that intermediate remainsFP32. A separate prefill-beta discrepancy remains: the native preparation rounds sigmoid output toBF16 then widens, while the reference storesFP32beta. Decode has a different rounding contract and must remain separate. Both saved-input replays completed but failed their frozen acceptance checks. Partial results cannot qualify the model; classification of the failed exact-reference reconstruction gates is in progress. No production fix or quality claim follows from these runs.

Root continues the full serving contract using existing vLLM bench serve harnesses. Gemma12BF16 at4K/C16 completed32measured/16warmup,512output,APCoff: Plow TPOT65.535ms vs vLLM15.021ms; p99ITL240.175ms vs16.267ms; output228.362 vs911.557tokens/s. Both32requests have exact lengths and zero failures. The original Plow startup failed from root Nix checkout ownership; exact checkout trust repair passed, and unchanged binaries/settings completed the retry. Actual order is vLLM then Plow. Previous1K/C1,C4,C16 Plow cells also remain slower than matched vLLM. Full15-cell repeated coverage and other models/contexts remain incomplete.
