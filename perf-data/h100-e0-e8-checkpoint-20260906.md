# H100 E0–E8 checkpoint, 2026-09-06

Main freeze is `9dbaa52`; experiments continue on `perf/h100-native`. No new kernel default is promoted. The all-model, all-metric vLLM goal is incomplete.

## Decisions from measurements

- E0 measured 3.154 TB/s sustained HBM on this SXM; one-CTA synthetic read reaches 2.752 TB/s. Old power-capped NVL occupancy conclusions are insufficient. Actual Gemma12 projection shapes gained roughly 9–12% from higher grid concurrency; cp.async depth6 was mostly parity at the best grid. This does not establish equivalent interpreter occupancy.
- E0 joins all 15,456 prefill graph nodes to packets. HD512 full attention is 55.29% of inclusive kernel time, sliding attention 10.77%, GEMM 20.89%, and merge 7.53%. Prioritize E4 before E5. Decode block0 gate share is 14.9/14.7% at 1K/32K; this is a diagnostic sample, not a complete per-op wall-time budget.
- E1 tensor-core QK+PV passes standalone numerical gates. It remains slower than the actual library references (FA4 for Gemma HD512, FA3 for Qwen HD256). Split-count selection improves coverage, but posthoc selection is not a validated packet planner. Broad-interpreter PV and QK-only candidates subsequently failed the fixed full-model numerical gate; timing is held.
- E3 four-stage skinny GEMM passed 2,560 correctness rows and 1,008 timing rows, including zero, split-K compute, and finalize. Posthoc median speedups over native scalar M4/8/16/32 are 1.54/2.02/3.57/6.34×; versus cuBLASLt they are 0.955/0.941/0.939/0.874×. Held-repeat and held-shape results are retained separately. No M1 measurement, fused GLU/QKV gate, local reduce packet integration, or serving qualification yet.
- E4 HD512 four-arm comparison passed all 64 full rows. WG64 stage1 is 2.98–3.45× faster than the deployed standalone body, but still 1.61–3.48× FA4 across tested Gemma cells. Same NS3 and merge included. The actual role subsequently failed its first full-model gate and has spills. A standalone kernel result is not TTFT.

## First serving cells

Gemma12 native BF16, input1024/output512, seed42, APC off, 32 measured/16 warmup requests per cell. All 96 measured requests have exact lengths and no failures. One sweep only; reverse controls and the remaining 12 cells are pending.

| Concurrency | Mean TTFT ms | Mean TPOT ms | p99 ITL ms | Output tokens/s |
|---:|---:|---:|---:|---:|
| 1 | 84.85 | 12.645 | 12.71 | 78.21 |
| 4 | 172.52 | 23.658 | 28.50 | 166.95 |
| 16 | 545.46 | 57.003 | 134.59 | 274.26 |

The coherence curl uses the previously qualified explicit BOS prompt. The random benchmark workload was unchanged. CPU tokenizer audit checks 160 actual generated prompts at five input lengths and finds identical token IDs; server-level tokenization verification remains pending. Fresh vLLM startup was repaired by adding /opt/pytorch/bin to sudo PATH; all three same-contract control cells then completed. Failed logs and separate retry outputs are retained. Plow is slower in all three cells; reverse-order repetition is pending.

## Qwen quality

The opaque layer0 capture preserves both original optimized-reference full-logit rows exactly, and captured repeats are exact. Native QKV/Z projections differ by relative L2 0.00400/0.00536, recurrent output by 0.00447, and recurrent state by 0.01734. These are diagnostic differences without new pass tolerances. Exact loaded layer0 weights/scales exclude a payload mismatch there; normalized inputs were not captured. The previous full-model discrepancy remains unresolved.

## Work allocation and promotion

E1 agent isolates the failed full-model arithmetic change. E4 agent checks the actual NS9 short-bucket failure and prepares saved-input replay. E6 agent validates claimed-packet prefetch and the two expected optional-symbol API probes, preserving counter semantics. Root serializes GPU jobs using existing harnesses and logs clocks/power. No speculative lookahead into unclaimed queue entries.

Promotion requires full-model numerical and reset gates, then repeated alternating 15-cell serving comparisons at 32 measured/16 warmup requests. E3 needs a generic scratch/reduction packet contract before runtime integration. E2 planner, E5, E7 SM partition, E8 matched W8A8, Gemma31/Qwen serving grids and larger contexts remain pending. Requested live KV rejects unsupported layouts through propagated errors; it does not silently fall back. Compiler-emitted KV geometry and flag consolidation remain follow-ups; this checkpoint changes evidence only.

## Integration gates and fresh controls, 02:36 UTC

Fresh vLLM startup retry completed all96requests with exact lengths. C1/C4/C16 meanTPOT10.590/11.000/12.808ms vs Plow12.645/23.658/57.003ms; output throughput93.77/356.08/1173.05 vs78.21/166.95/274.26tokens/s. All three Plow cells are slower. This is one engine order, not final repeated15-cell qualification.

E1 broad integration captured75finite full-logit rows. All15default/equal-arena checks,30repeat rows and6unchanged-prefill comparisons are exact. All30cross-arm argmax values agree, but8repeated continuation comparisons exceed the frozen1% relativeL2 limit; maximum2.584%. Centered differences remain significant. Numerical gate FAILED; no timing. Next isolate QK-only from PV rounding/grouping with the same threshold and packet.

E4 actual role has255registers,288Bstack,300Bspill stores/528Bspill loads and a WGMMA serialization warning. Its rebuilt control executable sections exactly match deployed code. The initial128-token full-model gate FAILED: maximum fullL2.03515, centeredL2.10436 andone of five argmax values changes. All later captures/timings were skipped by prerequisites. This does not implicate the unchanged production control; standalone correctness did not establish integration correctness. Agent audits packet tile/partial geometry before further numerical isolation.

E6 frozen claimed-packet prefetch retains200registers/zero spills; all5,397 CPU bounds/thread cases pass. The first four full-logit captures passed, including ten exact candidate snapshots at1K/32K. The memcheck process returned99 with two missing optional packet-hash-symbol API probes; unfiltered control classification remains pending. No device-memory pass or timing is claimed yet. The E3 integration plan requires generic zero, split-K and FP32-to-BF16 finalizer packets, explicit scratch lifetime and gates, then rung handling. The standalone528-CTA result must be requalified in132-CTA interpreter dispatch.

QK-only numerical isolation reused nine hash-verified controls and ran six fresh captures; it also fails the unchanged full-model gate. This rejects PV rounding as the sole explanation. E4 packet audit corrects the small-bucket assumption:128rows useNS9,512useNS5,1024useNS3. All arm pairs preserve those packet values. CPU ownership and partial capacities pass, so the next screen targets the NS9 tail path before real layer5 saved-input replay.
