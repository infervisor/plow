# H100 experiment review and next program

The goal remains matched vLLM performance across Gemma4 12B/31B and Qwen3.8 27B, all contexts and latency, throughput and tail metrics. No overall win is established. Projected timings and timelines in the proposed program are hypotheses.

## Review corrections from current evidence

- Matched C1 screening references now exist for all three models: four measured requests plus one warmup, identical input/output lengths, vLLM bench serve. They are not the required final 32/16-request grid.
- GEMV512 is integrated and passes full-logit parity through32K. Repeated Gemma12 scheduler-inclusive decode regresses about3.4% at1K and3.1% at32K. Gemma31 improves about2.6% at1K and2.5% at32K. Primitive gains do not justify global promotion.
- WS384 cursor has a measured serving effect, but small: two reversed runs improve Gemma12 TTFT0.8–1.5%, with decode unchanged.
- Current attention reuse improves full-block decode at32K: Gemma31 1.07×/1.12× forB1/B2; QwenFP8 1.25× atB1. No library win follows. QwenNS11→33 also changes logits and one tested8K argmax; sameNS GF2/GF6 remains bit-exact.
- Qwen independent FP8 quality is not qualified:11/15 teacher-forced histories agree on argmax; all15 reference repeat pairs are bit-exact. Worst1K centered full/head64 relativeL2 is0.700/0.254. CPU layer0 serialized weight payloads match the quantization recipe; actual loaded layer0 payload and scalar-scale comparison now passes exactly (383,221,760 FP8 bytes); activation/recurrent tracing is queued.
- Requested live KV fails explicitly on unsupported layouts. The inspected `run()?` propagates validation failure; it does not silently disable requested live mode. Compiler-emitted KV geometry remains a useful follow-up, with runtime validation retained.
- Current hardware reports H10080GB HBM3,132SMs,700W configured limit,2619MHz memory and up to1980MHz SM. Measured pure-read ceiling is3.154TB/s at stable1830MHz SM and2619MHz memory; oneCTA/SM reaches2.752TB/s. Actualshape occupancy is queued. Old NVL conclusions are not accepted as current measurements.
- Raw checks, logs, JSON and binaries were copied to campaign NVMe:9.26GB,10,795 regular files at snapshot time. New/active results need resync. This is instance-local storage, not off-machine archival.

## Execution assignments

| Work | Owner | Immediate deliverable |
|---|---|---|
| E0 | fp8_pipeline_audit | Checked HBM ceiling, reversed occupancy sweeps, current clocks/power; actual-shape occupancy and Gemma12 op traces next |
| E1 | gemm_asm_opt | Artifact-only GQA-packed tensor-core QK, then separate tensor-core PV/ring arm; current library comparator |
| E2/E3/E6 | gdn_prefill_debug | Existing cp.async ring probe; rung-aware planner and pipelined skinny-GEMM design; bounded next-packet prefetch after gate-budget measurement |
| E4/E5 | Ranked by E0 | HD512/HD256 prefill attention and pipelined GEMM experiments against current libraries |
| E7/E8 | After prerequisite kernels | Green-context interference probe, then precision-matched GemmaW8A8 |

E1 begins with numerical isolation (tensor-core QK, unchanged FP32 softmax/PV and merge), but the proposed QK+PV target remains. E3 must coverM32 explicitly; a literalBM16-only port misses rows or duplicates weight traffic. F32 atomics require bounded-error and repeat-drift gates. E6 must not speculate into unmapped KV or alter synchronization semantics.

## Promotion and merge criteria

All new kernel work remains in standalone probes until qualified. Existing harnesses, correctness before timing, alternating order, at least two repeats, symmetric eviction and contemporaneous clocks/power apply to kernel comparisons. Serving promotion requires inputs1024/4096/8192/16384/32768 × concurrency1/4/16,512 output,32 measured requests/16 warmups, matched precision/tokenization/cache policy, all errors and individual lengths checked. Longer64K/128K/native-limit contexts remain in scope.

No default promotion or deletion of a still-needed control is justified solely by a primitive result. Retire losing flags with their replacement evidence and preserve the archived controls. In particular, keep GEMV512 off for Gemma12; do not iterate on unpipelinedM16 as the E3 solution. HD256 attention role and FP8 cursor promotion still require the full serving contract.

Merged reviewed source9dbaa52 to main by fast-forward. 1,329 workspace tests pass at6c88761;399 CUDA/HSA/hub runtime tests pass after9dbaa52; Nix build passes. Both lazy-ring GPU parity gates pass. Review found and fixed unsafe live-KV/multistep combination; no other introduced merge blocker found. Further experiment and diagnostic commits continue on perf/h100-native. Full serving promotion remains pending.

E1 tensor-core QK passes148 repeated operator cells but wins0 library cells (9/72 candidate cells within1.3×). Tensor-core PV now passes72/72 candidate numerical cells:15/36 distinct batch/context cells meet≤1.3×library inbothorders, still0librarywins. Actual current reference is FA4 forGemmaHD512 andFA3 forQwenHD256; do not labelallFA3. E2 depth2/4/6 cp.async passes54 exact cells but does not justify a production port; depth6 is roughly parity. E0 occupancy favors2–4gridCTAs onlargeprojections overcp.async alone. Gemma12 block0 trace gate14.9/14.7% at1/32K (uncapped); fullwallbudgetnotyetqualified.

Graph-node attribution now validates all15,456 nodes against32×483 packet segments. Gemma12 32K prefill span5,821.121ms: fullHD512 attention3,202.986ms, slidingHD256 attention623.914ms, allGEMM1,210.405ms, FlashMerge436.079ms. E4 fullattention ranks first; E5 GEMM second. Existing HD512 library comparator already usesWGMMA; audit the deployed role before describing a new WGMMA port.
