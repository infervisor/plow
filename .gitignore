# Local-only plans / research scratchpad for coding agents
plans/

# Local re-runs of perf campaign dumps — do not re-commit
perf-data/**/*-raw*.txt
perf-data/**/*-raw-*.txt
perf-data/**/*.jsonl
!perf-data/block-configs/**

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
target/
plow-out/
build/.claude/worktrees/
runtime/ubench/build/

# Claude Code agent worktrees + session state (lives at .claude/, not build/.claude/)
.claude/
build/
build-*/
*.log
__pycache__block-assets/
__pycache__

# Isolated cargo target for `--features hsa` builds, so a concurrent agent
# rebuilding the shared `target/` without that feature cannot break amd-bench.
target-hsa/
ba_agt/
ba_ctl/
ba_hier/
ba_hier2/
ba_keeprel/
ba_l2/
ba_l2hier/
ba_main/
ba_sc1/
ba_sc1g/
# every cmake hsaco build dir from the HIER2/SC1 campaign; objects, not source
ba_*/
gpucore.*.gpu

# committed-by-accident artifacts (binaries/profiles live outside git)
cuda-keyring_*.deb
plow_profile.sqlite

# GPU binary build outputs — never check in (bundles link them from build dirs)
*.cubin
*.hsaco

# nix build output
result/
