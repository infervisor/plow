--- a/runtime/amd/interp.hip
+++ b/runtime/amd/interp.hip
@@ -5104,6 +5104,17 @@
         __asm__ __volatile__("s_icache_inv" ::: "memory");
 #endif
 #if defined(__gfx942__) && PLOW_GLOBAL_QUEUE && !PLOW_BUCKET_PREFILL && \
+    !PLOW_BUCKET_FLASH && !PLOW_MIXED_STEP && PLOW_GEMV_MM == 4 && \
+    !PLOW_FP8 && !PLOW_FP8_KV && !PLOW_MXFP4 && \
+    !PLOW_MLA_PREFILL && !PLOW_K3 && \
+    (!PLOW_DECODE_INVENTORY_PRUNE || PLOW_HAS_FLASH_DECODE)
+        if (in->op == PLOW_DOP_FLASH_DECODE) {
+            const CInst flash_in = plow_scalar_ptr(in);
+            exec_flash_decode(flash_in, plow_scalar_ptr(tens), e.slice, flash_in->blocks,
+                              sm.fd, flash_in->i[0]);
+        } else
+#endif
+#if defined(__gfx942__) && PLOW_GLOBAL_QUEUE && !PLOW_BUCKET_PREFILL && \
     !PLOW_BUCKET_FLASH && !PLOW_MIXED_STEP && \
     (PLOW_GEMV_MM == 1 || PLOW_GEMV_MM == 4) && \
     !PLOW_FP8 && !PLOW_FP8_KV && !PLOW_MXFP4 && \
