--- a/runtime/amd/interp.hip
+++ b/runtime/amd/interp.hip
@@ -5105,7 +5105,33 @@
         if (in->op == PLOW_DOP_GEMV) {
             const CInst gemv_in = plow_scalar_ptr(in);
             exec_gemv(gemv_in, plow_scalar_ptr(tens), e.slice, gemv_in->blocks, sm.gm);
-        } else
+        }
+#if PLOW_GEMV_MM == 1 && (!PLOW_DECODE_INVENTORY_PRUNE || PLOW_HAS_GEMV_GLU)
+        else if (in->op == PLOW_DOP_GEMV_GLU) {
+            const CInst gi = plow_scalar_ptr(in);
+            const CTens gt = plow_scalar_ptr(tens);
+#define FAST_TEN(k) (gi->t[k] == PLOW_TENSOR_NONE ? nullptr : gt[gi->t[k]])
+            d_gemv_glu((bf16*)FAST_TEN(0), (const bf16*)FAST_TEN(1),
+                       (const bf16*)FAST_TEN(2), (const bf16*)FAST_TEN(5), gi->i[0],
+                       gi->i[1], gi->i[2], gi->i[5], e.slice, gi->blocks, sm.gm,
+                       gi->fj[0].f, gi->fj[1].f);
+#undef FAST_TEN
+        }
+#endif
+#if PLOW_GEMV_MM == 1 && (!PLOW_DECODE_INVENTORY_PRUNE || PLOW_HAS_GEMV_QKV)
+        else if (in->op == PLOW_DOP_GEMV_QKV) {
+            const CInst gi = plow_scalar_ptr(in);
+            const CTens gt = plow_scalar_ptr(tens);
+#define FAST_TEN(k) (gi->t[k] == PLOW_TENSOR_NONE ? nullptr : gt[gi->t[k]])
+            d_gemv_qkv((bf16*)FAST_TEN(0), (bf16*)FAST_TEN(3), (bf16*)FAST_TEN(5),
+                       (const bf16*)FAST_TEN(1), (const bf16*)FAST_TEN(2),
+                       (const bf16*)FAST_TEN(4), (const bf16*)FAST_TEN(6), gi->i[0],
+                       gi->i[1], gi->i[3], gi->i[4], gi->i[2], e.slice, gi->blocks,
+                       sm.gm, (const bf16*)FAST_TEN(7), gi->fj[0].f);
+#undef FAST_TEN
+        }
+#endif
+        else
 #endif
         plow_exec(in, e.slice, tens, &sm, &prog);
 #endif
