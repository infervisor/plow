[package]
name = "costmodel"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared hardware cost model (design §6.3): MMA tile shapes, paged SRAM, and cycle/DMA estimates over a hwspec GpuSpec"

[dependencies]
hwspec.workspace = true
nn-graph = { workspace = true, default-features = false, features = [] }
